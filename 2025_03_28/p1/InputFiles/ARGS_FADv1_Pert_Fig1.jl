# Perturbation details
#=pert = (p   = iARG.pp,	# Parameter to be perturbed
		d   = 1.05,		# Perturbation size (Delta rho)
		c   = iARG.ax,	# Condition parameter
		r   = [-2.0,2.0],	# Range of conditions
		s   = 0.2,		# Step   0.2 normal
		eps = 0.5);		# CoRa threshold
		pc  = iARG.pp   # Parameter to obtain one cora value
=#
pert = (p   = iARG.pp,	# Parameter to be perturbed
d   = 1.05,		# Perturbation size (Delta rho)
c   = iARG.ax,	# Condition parameter
r   = [-3.0,3.0],	# Range of conditions
s   = 1,		# Step
eps = 0.1);		# CoRa threshold
